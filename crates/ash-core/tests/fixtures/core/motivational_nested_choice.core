(handle (clause (cap choice.pick : (Unit) -> Int) ((seed : Unit)) (resume outer : (cont Int Int {} multi-shot-pure)) : {} (let-cont-call a outer (lit-int 1) (let-cont-call b outer (lit-int 2) (let-prim total add (a b) total)))) (handle (clause (cap choice.pick : (Unit) -> Int) ((seed : Unit)) (resume inner : (cont Int Int {} multi-shot-pure)) : {} (let-cont-call c inner (lit-int 10) (let-cont-call d inner (lit-int 20) (let-prim total add (c d) total)))) (raise (cap choice.pick : (Unit) -> Int) ((lit-unit)))))
