# TASK-377: Ignore Caches and Build Artifacts

## Status: ✅ Complete

## Description

Update repository ignore rules so local runtime state, Python caches, test caches, Ruff caches, and generated packaging/build artifacts do not appear as untracked noise during normal Ash and `tools/agent-pipeline` development.

## Specification Reference

- `.gitignore`: repository-wide ignore policy
- `tools/agent-pipeline/`: Python package and local runtime artifact sources
- AGENTS.md: changelog/task-file expectations for workflow/tooling changes

## Dependencies

- ✅ TASK-371: Agent Pipeline Supervision Fixes
- ✅ TASK-372: Agent Pipeline Packaging Review Fixes
- ✅ TASK-373: Agent Pipeline Quality Contracts
- ✅ TASK-374: Agent Pipeline Configurable Stage Agents
- ✅ TASK-375: Agent Pipeline Status Effective Stage Agents
- ✅ TASK-376: Agent Pipeline Status Runtime Mapping and CLI Errors

## Requirements

1. Repository `.gitignore` must exclude Python cache/build artifacts generated by agent-pipeline work.
2. Repository `.gitignore` must exclude local `.agents` runtime state and placeholder temp directories used by the pipeline.
3. The ignore changes must reduce current untracked-noise without hiding source files that should remain tracked.
4. Changelog/task tracking must record the tooling/workflow ignore update.

## TDD Steps

1. Inspect current untracked cache/runtime/build artifacts.
2. Update `.gitignore` with the minimum rules needed to ignore them.
3. Re-run `git status --short` to verify the targeted artifacts disappear from untracked output.

## Completion Checklist

- [x] Python cache/build artifacts ignored
- [x] Agent-pipeline runtime state/temp artifacts ignored
- [x] `git status` no longer shows the targeted generated artifacts as untracked
- [x] `CHANGELOG.md` updated
