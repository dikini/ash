(handle (clause (operation kv.read : (String) -> String) ((key : String)) (resume resume : (cont String String {operation audit.emit} multi-shot-pure)) : {operation audit.emit} (jump resume key)) (raise (operation kv.read : (String) -> String) ((lit-string "user:7"))))
