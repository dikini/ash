(handle (clause (cap kv.read : (String) -> String) ((key : String)) (resume resume : (cont String String {cap audit.emit} multi-shot-pure)) : {cap audit.emit} (jump resume key)) (raise (cap kv.read : (String) -> String) ((lit-string "user:7"))))
