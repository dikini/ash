# Appendix A: Example Inventory

This appendix is historical orientation only. The pre-Phase-201 workflow-era example catalog was
removed from the productive repository because it preserved deleted source forms.

Current checked examples are intentionally small and target-only:

| Example | Purpose | Gate |
| --- | --- | --- |
| `examples/10-testing-helpers/testing_helpers.ash` | Productive testing-helper library surface | `ash check` through the example corpus |
| `examples/11-process-channel-helpers/process_channel_helpers.ash` | Productive process/channel helper surface | `ash check` through the example corpus |

Use `docs/TUTORIAL.md`, `docs/tutorials/phase199-productive-apps.md`, and `examples/README.md`
for current example guidance. Historical workflow catalog names are not current examples and must
not be copied into new Ash code.
