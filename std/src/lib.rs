//! Rust crate wrapper for the Ash standard library sources.
//!
//! The canonical standard library surface lives in the sibling `.ash` source files under
//! `std/src/`. This crate exists so the workspace can build and package that source tree.
