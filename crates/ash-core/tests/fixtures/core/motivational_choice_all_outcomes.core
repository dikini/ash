(handle (clause (operation choice.pick : (Unit) -> Int) ((seed : Unit)) (resume resume : (cont Int Int {} multi-shot-pure)) : {} (let-cont-call left resume (lit-int 1) (let-cont-call right resume (lit-int 2) (let-prim total add (left right) total)))) (raise (operation choice.pick : (Unit) -> Int) ((lit-unit))))
