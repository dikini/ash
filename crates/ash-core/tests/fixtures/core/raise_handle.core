(handle
  (clause (operation console.read : (String) -> Unit) ((line : String)) (resume k : (cont Unit Unit {} affine)) : {}
    (jump k (lit-unit)))
  (raise (operation console.read : (String) -> Unit) ((lit-string "ok"))))
